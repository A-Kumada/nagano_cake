<div class="container">
  <div class="row">
    <div class="col-md-10">

<h4 class="pt-2 pb-3">ログイン</h4>
  <h5 class="offset-1 pb-3">会員の方はこちらからログイン</h5>
    <div class="offset-3">

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

    <table>
      <div class="field">
        <tr>
          <td width="180 pb-2"><%= f.label :"メールアドレス" %></td>
          <td class="pb-2"><%= f.email_field :email, autofocus: true, autocomplete: "email",:size=>"50" %></td></tr>
      </div>
      <div class="field">
        <tr>
          <td class="pb-5"><%= f.label :"パスワード" %></td>
          <td class="pb-5"><%= f.password_field :password, autocomplete: "current-password",:size=>"50" %></td></tr>
      </div>
      <tr><td></td><td><%= f.submit "ログイン",class:"btn btn-primary" %></td></tr>
  <%# if devise_mapping.rememberable? %>
    <%#div class="field">
      <%= f.check_box :remember_me %>
      <%#= f.label :remember_me %>
    <%#/di>
  <% end %>
    </table>
    <% end %>
    </div>
    </div>
  </div>

  <div class="row">
    <div class="col-md-10 pt-5">
      <h5 class="offset-1 pb-3">会員登録がお済みでない方は</h5>
        <div class="offset-3">
         <%= link_to "こちら", new_customer_registration_path %>　から新規登録を行ってください。
       </div>
  </div>
</div>